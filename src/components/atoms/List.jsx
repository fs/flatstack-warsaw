import styled from 'styled-components';

const List = styled.ul`
  margin: 1em 0;
  padding: 0 0 0 2em;

  list-style: disc;
`;

List.Item = styled.li`
  margin: 0 0 0.5em 0;
`;

export default List;
